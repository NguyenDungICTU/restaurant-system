import { useEffect,useState } from 'react'
import { BellOutlined,CalendarOutlined,DashboardOutlined,MenuFoldOutlined,MenuUnfoldOutlined,SettingOutlined,ShoppingCartOutlined,TeamOutlined,UnorderedListOutlined,UserOutlined,WifiOutlined,DisconnectOutlined,LogoutOutlined,ApartmentOutlined } from '@ant-design/icons'
import { Button,Tooltip } from 'antd'
import { createOrderSocket,getHealth,logout } from '../services/api'
import KhuVuc from '../pages/KhuVuc'
import Ban from '../pages/Ban'
const navigation=[{key:'dashboard',label:'Tổng quan',icon:DashboardOutlined},{key:'bookings',label:'Đặt bàn',icon:CalendarOutlined},{key:'customers',label:'Khách hàng',icon:TeamOutlined},{key:'menu',label:'Thực đơn',icon:UnorderedListOutlined},{key:'orders',label:'Đơn hàng',icon:ShoppingCartOutlined},{key:'areas',label:'Khu vực',icon:ApartmentOutlined},{key:'tables',label:'Quản lý bàn',icon:UnorderedListOutlined}];const secondary=[{key:'settings',label:'Cài đặt',icon:SettingOutlined}]
export default function RestaurantShell({user,onLogout}){const[page,setPage]=useState('dashboard'),[collapsed,setCollapsed]=useState(false),[health,setHealth]=useState('checking'),[socket,setSocket]=useState('connecting'),[events,setEvents]=useState([]);useEffect(()=>{let m=true;getHealth().then(()=>m&&setHealth('online')).catch(()=>m&&setHealth('offline'));return()=>{m=false}},[]);useEffect(()=>{let s;try{s=createOrderSocket(msg=>setEvents(x=>[{id:`${Date.now()}-${Math.random()}`,message:msg},...x].slice(0,5)),setSocket)}catch{setSocket('error')}return()=>s?.close()},[]);async function signout(){try{await logout()}finally{onLogout()}}const label=[...navigation,...secondary].find(x=>x.key===page)?.label||'Tổng quan';return <div className="app-shell"><aside className={`sidebar ${collapsed?'collapsed':''}`}><div className="brand"><div className="brand-mark">R</div>{!collapsed&&<div><strong>Resto</strong><span>Management</span></div>}</div><div className="nav-section">{!collapsed&&<p className="nav-title">QUẢN LÝ</p>}{navigation.filter(item=>item.key!=='tables'||user?.role==='QUAN_LY').map(({key,label:txt,icon:Icon})=><button key={key} className={`nav-item ${page===key?'active':''}`} onClick={()=>setPage(key)} title={collapsed?txt:undefined}><Icon/>{!collapsed&&<span>{txt}</span>}</button>)}</div><div className="sidebar-bottom">{!collapsed&&<p className="nav-title">HỆ THỐNG</p>}{secondary.map(({key,label:txt,icon:Icon})=><button key={key} className={`nav-item ${page===key?'active':''}`} onClick={()=>setPage(key)}><Icon/>{!collapsed&&<span>{txt}</span>}</button>)}<button className="nav-item logout-nav" onClick={signout}><LogoutOutlined/>{!collapsed&&<span>Đăng xuất</span>}</button></div><div className="user-card"><div className="avatar"><UserOutlined/></div>{!collapsed&&<div className="user-copy"><strong>{user?.full_name||'Nhân viên'}</strong><span>{user?.role||'Nhân viên'}</span></div>}</div></aside><main className="main-content"><header className="topbar"><Button type="text" className="collapse-button" icon={collapsed?<MenuUnfoldOutlined/>:<MenuFoldOutlined/>} onClick={()=>setCollapsed(!collapsed)}/><div className="breadcrumb"><span>Nhà hàng</span><b>/</b><strong>{label}</strong></div><div className="topbar-actions"><div className="connection-status"><span className={`status-dot ${health==='online'?'success':health==='offline'?'danger':'warning'}`}/><span>API {health==='online'?'Online':health==='offline'?'Offline':'Đang kiểm tra'}</span></div><Tooltip title="Thông báo"><Button type="text" icon={<BellOutlined/>}/></Tooltip><div className="top-avatar">{(user?.full_name||'A').slice(0,1).toUpperCase()}</div></div></header><div className="page-content">{page==='dashboard'?<Dashboard health={health} socket={socket} events={events}/>:page==='areas'?<KhuVuc/>:page==='tables'?<Ban/>:<ModulePage page={page}/>}</div></main></div>}
function Dashboard({health,socket,events}){const cards=[['Đặt bàn hôm nay','—','Chưa có API đặt bàn'],['Khách hàng','—','Chưa có API khách hàng'],['Đơn hàng','—','Dữ liệu realtime qua WebSocket'],['Doanh thu','—','Chưa có API báo cáo']];return <><section className="page-heading"><div><p className="eyebrow">RESTAURANT MANAGEMENT</p><h1>Tổng quan hoạt động</h1><p className="subheading">Theo dõi nhanh tình trạng hệ thống và các nghiệp vụ nhà hàng.</p></div><div className="live-pill"><span className={`status-dot ${socket==='connected'?'success':'warning'}`}/><WifiOutlined/> WebSocket {socket==='connected'?'đã kết nối':'chưa kết nối'}</div></section><section className="stats-grid">{cards.map(([a,b,c])=><article className="stat-card" key={a}><div className="stat-label">{a}</div><div className="stat-value">{b}</div><div className="stat-note">{c}</div></article>)}</section><section className="content-grid"><article className="panel"><div className="panel-heading"><div><h2>Trạng thái hệ thống</h2><p>Các kết nối hiện có trong backend.</p></div><WifiOutlined className="panel-icon"/></div><div className="system-list"><div><span><span className={`status-dot ${health==='online'?'success':'danger'}`}/> API Health</span><strong className={health==='online'?'text-success':'text-danger'}>{health==='online'?'Đang hoạt động':'Cần kiểm tra'}</strong></div><div><span><span className={`status-dot ${socket==='connected'?'success':'warning'}`}/> WebSocket /ws/orders</span><strong>{socketLabel(socket)}</strong></div><div><span><span className="status-dot warning"/> CRUD nghiệp vụ</span><strong className="text-muted">Chưa có endpoint</strong></div></div></article><article className="panel"><div className="panel-heading"><div><h2>Order realtime</h2><p>Sự kiện nhận từ WebSocket.</p></div><ShoppingCartOutlined className="panel-icon"/></div>{events.length?<div className="event-list">{events.map(e=><div className="event-item" key={e.id}><span>{e.message}</span><small>vừa nhận</small></div>)}</div>:<div className="empty-state"><DisconnectOutlined/><strong>Chưa có sự kiện</strong><span>Khi backend broadcast order update, dữ liệu sẽ xuất hiện tại đây.</span></div>}</article></section><section className="implementation-note"><div className="note-icon">i</div><div><strong>Frontend đang bám đúng API backend hiện có</strong><p>Đăng nhập thật qua <code>/api/auth/login</code>, kiểm tra phiên qua <code>/api/auth/me</code>, đăng xuất qua <code>/api/auth/logout</code> và realtime order qua <code>/ws/orders</code>. Các module còn lại chưa gọi API giả.</p></div></section></>}
function socketLabel(s){return s==='connected'?'Đã kết nối':s==='error'?'Lỗi kết nối':'Đang kết nối'}function ModulePage({page}){const labels={bookings:['Đặt bàn','Quản lý lịch đặt bàn, khung giờ nhận khách và trạng thái bàn.'],customers:['Khách hàng','Quản lý hồ sơ, thông tin liên hệ và lịch sử khách hàng.'],menu:['Thực đơn','Quản lý nhóm món, món ăn, giá và trạng thái còn/hết trong ngày.'],orders:['Đơn hàng','Theo dõi order theo bàn và tiến độ phục vụ.'],settings:['Cài đặt','Cấu hình các thông tin vận hành của nhà hàng.']};const[t,d]=labels[page]||['Tổng quan',''];return <section className="module-placeholder"><div className="placeholder-icon"><SettingOutlined/></div><p className="eyebrow">MODULE</p><h1>{t}</h1><p>{d}</p><span>UI đã sẵn sàng · Chờ backend cung cấp endpoint nghiệp vụ.</span></section>}
