# Changes in fixed build

- Restored complete Login page CSS.
- Removed external login image and Google Fonts dependency so the demo does not rely on those assets being reachable from the browser.
- Added CRUD API for menu dishes and required category relation.
- Added frontend management for dishes under each menu category.
- Added idempotent demo menu seed data.
- Added README with backup, overwrite, Docker rebuild, and GitHub clone instructions.
